<div>
<div class="card">
    <div class="card-header">
      {{commentBlogs.post_title}}
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p>{{commentBlogs.post_content}}</p>
        <footer class="blockquote-footer">Created by: <cite title="Source Title">{{user.username}}</cite> on {{format_date commentBlogs.post_created}}</footer>
      </blockquote>
       <div class="col-md-4">
        <input class="form-input" type="text" id="comment" name="content-body"/>
        <button class="btn btn-sm btn-primary blog-list" id="cmtclick" data-id="{{blog.id}}">COMMENT</button>
      </div>
    </div>
  </div>
</div>
<div>{{commentBlogs.comment.post_comment}}</div>
{{!-- {{#if commentBlogs }}
{{post_comment}}
{{comment}}
        {{#each commentBlogs as |comment|}}
        {{comment}}
        <div></div>
        <p>{{comment.post_comment}} by: {{commentBlogs.user.username}} on {{blog.post_created}}</p>
        </div> 
        {{/each}}
        {{/if}} --}}

<script src="/js/comment.js"></script>